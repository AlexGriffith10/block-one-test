import React from 'react'
import {HEADER_TITLE} from '../../constants/Constants';

function Header() {
    return (
        <div>
            <h1>{HEADER_TITLE}</h1>
        </div>
    )
}

export default Header
