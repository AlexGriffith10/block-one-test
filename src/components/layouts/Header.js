import React from 'react'
import {HEADERTITLE} from '../../constants/Constants';

function Header() {
    return (
        <div>
            <h1>{HEADERTITLE}</h1>
        </div>
    )
}

export default Header
